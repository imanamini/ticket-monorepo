import { InternetService } from './internet.service';
import { TestBed } from '@angular/core/testing';

describe('Internet Service Tests', () => {
  let internetService: InternetService;

  beforeEach(() => {
    TestBed.configureTestingModule({ providers: [InternetService] });
  });

  it('test test', () => {
    // internetService.
    // console.log('it is a test test');
  });
});
